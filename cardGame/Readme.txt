pour compiler le programme:
     => lancer le makefile

pour executer le programme:
     => ./main

pour utiliser le programme:
     => suivez les instructions sur le Terminal
     => taper le chiffre associé pour selectionner un element afficher sur la console
     => taper "pioche" pour piocher une carte quand c'est possible
